import React from 'react';

const Orders = () => {
  return (
    <div>
      <h2>Manage Orders</h2>
      {/* Add logic to manage orders: View, Update Status, etc. */}
    </div>
  );
};

export default Orders;
